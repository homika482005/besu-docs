---
description: Product-specific guidance for public networks documentation, including terminology, content areas, and reference conventions.
globs:
  [
    'docs/public-networks/**/*.md',
    'docs/public-networks/**/*.mdx',
  ]
alwaysApply: false
---

# Public networks

## Scope

Public networks documentation covers running Besu on Ethereum Mainnet and public testnets. This
includes syncing, staking, API usage, monitoring, and development workflows.

## Key concepts

- **Execution client**: Besu is an execution client. It pairs with a **consensus client** (such as
  Teku, Lodestar, or Prysm) to operate a full Ethereum node post-Merge.
- **Sync modes**: snap sync (default) and full sync. Use these terms consistently.
- **JSON-RPC API**: the primary API for interacting with Besu. Reference pages live under
  `reference/api/`.
- **Engine API**: the API used for execution-consensus client communication. Reference pages live
  under `reference/engine-api/`.
- **CLI options**: command line options for configuring Besu. Reference pages live under
  `reference/cli/`.

## Content organization

| Folder          | Content type                                                |
|-----------------|-------------------------------------------------------------|
| `concepts/`     | Explanation (transactions, proof of stake, data storage, network architecture) |
| `get-started/`  | Onboarding (system requirements, installation, connecting to networks) |
| `how-to/`       | How-to (configure Besu, connect to networks, use the API, monitor, troubleshoot) |
| `reference/`    | API reference (JSON-RPC, Engine API, CLI options, genesis file) |
| `tutorials/`    | End-to-end walkthroughs                                     |

## Reference conventions

### JSON-RPC methods

- Each method page follows the pattern: method name as heading, description, **Parameters**,
  **Returns**, **Example** (with request and response).
- Use consistent formatting across all method pages. Check neighboring pages for the established
  pattern.

### CLI options

- Each option uses its long-form name without the `--` prefix as the heading (for example, `## data-path`).
- Include syntax, description, default value, and any environment variable equivalent.

## Configuration examples

- Configuration examples should show both command line options and TOML configuration file syntax
  where applicable. Use Docusaurus `<Tabs>` to present both formats.
- TOML configuration examples use the `.toml` language tag.

## Sidebar

The sidebar is autogenerated from the filesystem. Use `_category_.json` files to control ordering
and labels within directories.
